dvisR_system <- function(dat, cluster_labels = rep(NA, nrow(dat)),
                         feature_list = .grab_functions_in_package(),
                         system_options = list(NA), 
                         plotting_options = list(NA), 
                         debugging_inputs = list(NA)){
 # preprocessing, initialization stuff
 
 # start phase 1
 
 # start phase 2
 
 # learn classifier
 
 # prepare output
}